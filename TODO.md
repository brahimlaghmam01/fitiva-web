# TODO

- [x] Create an edit plan for updating hero-section phone images to use new local images from `dist/images/phone`.
- [ ] Copy/ensure the images from `dist/images/phone` are available under the Vite public path (likely `public/images/phone`).
- [x] Update `src/components/landing/hero-section.tsx` to use only the 4 local images inside the phone mock.
- [x] Verify hero section renders the 4 images (no broken URLs / 404s).

